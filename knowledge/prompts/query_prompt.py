"""查询流程相关提示词模板

定义LLM 调用所需的提示词模板。
"""

# 商品名称提取提示词模板

ITEM_NAME_USER_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的商品名称（item_names）。
1. 如果用户明确提到了商品名称，请提取出来。可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个"、"它"）或确认词（如"对的"、"是的"、"第一个"），请结合历史会话进行指代消解，提取上一轮助手提到的商品名称。
3. 如果无法确定商品名称，item_names 返回空列表。
4. 请重新改写用户的问题（rewritten_query），使其成为包含商品名称的独立完整问题。当用户说确认词时，rewritten_query 应为上一轮用户的原始问题。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["商品A", "商品B"],
    "rewritten_query": "关于商品A和商品B，..."
}}"""




HYDE_USER_PROMPT_TEMPLATE = """
请根据用户的问题，编写一段技术文档片段作为回答参考。
商品名称：{item_names}
用户问题：{rewritten_query}

要求：
1. 文档片段应当专业、准确，使用技术文档的正式风格
2. 内容应紧扣用户问题，提供具体操作步骤或技术说明
3. 适当使用专业术语，但避免过于晦涩
4. 篇幅控制在 200-300 字左右
"""

ANSWER_PROMPT = """你是一个智能助手，请根据参考内容回答用户的问题。

严格遵循以下规则：
1. 回答必须基于【参考内容】，不得编造不存在的事实、参数或操作步骤。
2. 每一条事实性陈述必须标注【知识库原文】。
3. 如果参考内容不足以回答用户问题，明确说明「知识库中未涵盖该内容」，不得凭常识补充。
4. 如果用户的问题需要通过图片来辅助说明（例如：外观、结构、接线、示意图等），图片只能来自于本地切片文本中的图片，请在答案最后追加一个独立的图片区块，格式严格如下：
【图片】
<图片URL1>
<图片URL2>
（每行一个URL；如果没有合适图片则不要输出【图片】区块）
5. 答案中不要出现「根据参考内容」等冗余表述，直接用【知识库原文】标签即可。

【参考内容】
{context}

【历史对话】
{history}

【相关商品/实体】
{item_names}

【用户问题】
{question}

请回答："""